version: "3"
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/app
    ports:
      - 8080:8080
    restart: always
    networks:
      - tienda_default
networks:
  tienda_default:
    external: true
